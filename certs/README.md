# SSL certificates for HTTPS

Place these files here so Nginx can serve HTTPS (port 443):

- **origin.pem** – certificate (e.g. Cloudflare SSL)
- **origin.key** – private key

See the project root **README.Docker.md** for how to obtain and use certificates.
